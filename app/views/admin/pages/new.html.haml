%h1 New page
= semantic_form_for [:admin, @page], :html => {:class => 'form-horizontal'} do |form|
  = render :partial => 'form', :locals => {:form => form}
  = form.actions do
    = form.action :submit, :as => :button
